node_modules/
lib/
.theia/
.cache/
.browser_modules/
dist/
examples/*/build/
apps/*/esbuild.mjs
apps/*/gen-esbuild*.mjs
apps/*/src-gen/
*.tsbuildinfo
.DS_Store
output/
.playwright-cli/

# runtime chat logs written by smoke/flow runs against the sample book
examples/sample-book/ai/chat/
examples/sample-book/ai/context-snapshots/

# Obsidian vault state (the doctor installs the companion plugin on demand)
.obsidian/

# runtime chat logs when the repo root itself is opened as a workspace
/ai/

# local edit backups from a hook
.claude/.edit-baks/

# MB3 memory-bank
memory-bank/

# generated documentation inputs/outputs (tech_spec §1.6)
# derived from every `id:`/preference declaration — noise in the diff of any
# unrelated change; `docs/coverage-report.md` IS committed, on purpose.
packages/manuscript-workspace/docs-inventory.generated.json
# derived from content/**; committing it invites "edited the .md, forgot to
# regenerate" drift.
packages/manuscript-workspace/src/browser/docs/docs-content.generated.ts
